function sayHello(params) {
    return "hello";
}

let relative = sayHello();
console.log(relative); // hello, because the function returns "hello"

// The function does not take any parameters, but it still returns a value.