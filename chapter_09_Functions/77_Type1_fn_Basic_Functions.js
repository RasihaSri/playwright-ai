// Define 
function greet () {
    console.log('Hello!');
}

// This is a basic Type 1 function, it does not take any arguments and does not return anything, it just performs an action (logging to the console)
// Call
greet(); // Hello!

// If we call before defining, it works because of hoisting
