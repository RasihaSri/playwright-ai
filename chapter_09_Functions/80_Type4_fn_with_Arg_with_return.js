function sum (a, b) {
    return a + b;
}

let result = sum(5, 10);
console.log(result); // 15, because the function returns the sum of a and b
let result2 = sum(20, 30);
console.log(result2); // 50, because the function returns the sum of a and b